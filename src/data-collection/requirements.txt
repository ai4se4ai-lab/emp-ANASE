# Analogy SE Data Collection Framework — Python dependencies
# Install with: pip install -r requirements.txt

# Core HTTP + data
requests>=2.31.0
pandas>=2.1.0
python-dotenv>=1.0.0

# Reddit (optional but recommended — falls back to public JSON without it)
praw>=7.7.0

# Analysis & statistics
scipy>=1.11.0
numpy>=1.26.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# NLP (optional — improves ERR noun/verb extraction)
nltk>=3.8.0
scikit-learn>=1.3.0

# Utilities
tqdm>=4.66.0
